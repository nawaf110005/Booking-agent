"""Booking-Agent — AI event-ticket booking concierge.

A chat-first agent that takes a user from intent ("I want a ticket") to a
confirmed, paid, emailed ticket: event search, member-discount pricing,
visual seat selection, atomic time-bounded seat holds, human-in-the-loop
payment confirmation, and HMAC-signed QR tickets.

See `.specify/memory/constitution.md` for the governing principles and
`specs/` for the per-feature specifications.
"""

__version__ = "0.1.0"
